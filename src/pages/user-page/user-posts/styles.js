export default (theme) => ({
  divider: {
    margin: '40px 0'
  },
  gridList: {
    width: '100%',
    height: '100%',
  },
  image: {
    cursor: 'pointer'
  },
  root: {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'space-around',
    overflow: 'hidden',
    backgroundColor: theme.palette.background.paper,
  },
});