export default (theme) => ({
  loading: {
    left: '50%',
    position: 'absolute',
    textAlign: 'center',
    top: '50%',
    transform: 'translate(-50%, -50%)'
  },
  loadingSmall: {
    display: 'flex'
  }
});
